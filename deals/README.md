# 案件管理ディレクトリ

## 概要
各投資案件を管理するディレクトリです。

## 構造
```
deals/
├── template/        # 案件テンプレート
└── [deal_name]/    # 案件単位のディレクトリ（動的に追加）
```

## 新規案件の作成
1. `template/` をコピーして新規案件ディレクトリを作成
2. 案件名をディレクトリ名に設定
3. 各ディレクトリに資料を配置

## 案件一覧
各案件は独立したディレクトリとして管理されます。
